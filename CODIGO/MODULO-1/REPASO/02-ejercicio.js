
const myNumber =5;

function numeroParImpar(numero) {
    if (numero % 2 == 0) return `El numero ${numero} es par`;
    else return `El numero ${numero} es impar`;
}

console.log(numeroParImpar(myNumber));
